name: Keep Streamlit Demo Awake!

on:
  schedule:
    # Every 15 minutes
    - cron: '*/15 * * * *' 
  workflow_dispatch: 

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping App URL
        run: curl -sSf https://stock-trading-dashboard.streamlit.app/ > /dev/null